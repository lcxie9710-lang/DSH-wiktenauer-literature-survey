# 全部文本文件在仓库里与工作区里都保持 LF。
#
# 为什么显式写：本机 git 是 core.autocrlf=true，默认会把检出的 .mjs 换成 CRLF ——
# 这些文件里有 `#!/usr/bin/env node` 之类的 shebang 场景，CRLF 在 Unix 上会直接把
# 解释器路径变成 `/usr/bin/env node\r`。统一 LF 省掉这一整类跨平台故障。
* text=auto eol=lf

# 二进制：别做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.zip binary
*.pdf binary
