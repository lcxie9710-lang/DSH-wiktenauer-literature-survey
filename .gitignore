# 依赖
node_modules/
.pnpm-store/

# lockfile：两个包都是**零外部依赖**（只 import node: 内置模块），所以 lockfile 里
# 三个 importer 全是空的，锁不住任何东西。留着它只会让"跑过一次 pnpm test"变成
# 工作区脏。哪天真的加了依赖，再把它从这行删掉即可。
pnpm-lock.yaml

# 运行产物（每次 run 的 trace / 审计 / JEV 调用日志都在这里）
out/

# 隔离的 DSH_HOME（CLI 路径用，不要把真实 ~/.dsh 搅进来）
.dsh-home/

# 密钥：JEV 走 AI Gateway，key 只放本地 .env，绝不进仓库
.env
.env.*
!.env.example

# 杂项
*.log
npm-debug.log*
coverage/
.DS_Store
Thumbs.db
.vscode/
.idea/

# 注意：**不要**忽略 `lib/`。
# v1 时代 lib/ 是 tsc 的编译产物，所以旧 .gitignore 忽略了它；
# 现在 packages/dsh-hema-v2/lib/ 是**手写的源码**，忽略了整个包就空了。
