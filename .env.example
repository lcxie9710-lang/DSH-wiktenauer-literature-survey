# JEV 走 Vercel AI Gateway，需要一个 gateway key。
#
# ⚠️ 放在哪里是有讲究的，放错位置不会报错 —— 只会让 JEV 调用全部失败，
#    症状和"没配 key"一模一样。本项目有**两条路径**，各认各的文件：
#
# ── A. GUI / preset 路径（你实际用的那条）──────────────────────
#    由 DSH 凭据服务解析，固定优先级：
#      1. 启动环境变量          AI_GATEWAY_API_KEY=... dsh web
#      2. <DSH_HOME>/.credentials.yaml   （GUI 设置界面写入的就是这里）
#      3. <invocation cwd>/.env          ← 你**启动 dsh 时所在目录**的 .env
#      4. <DSH_HOME>/.env
#    ★ 推荐写 <DSH_HOME>/.env（Windows: C:\Users\<你>\.dsh\.env）：
#      与 cwd 无关，配一次就不用再想。
#      若写仓库根的 .env（本文件所在的这一层），那就**必须从仓库根启动 dsh**，
#      否则第 3 层读到的是别的目录，key 根本不会生效。
#
# ── B. 无头 CLI 路径（harness/run.mjs）─────────────────────────
#    读 `packages/dsh-hema-v2/.env`（harness/env.mjs 的 ENV_PATH）或进程环境变量。
#    想跑 CLI 就把它放在那里，或者导出环境变量。
#
# 自检（会真调一次网关，验的是上面 A 那套链路；偶发 120s 超时，重跑即可）：
#   node packages/dsh-jev/verify-key.mjs
#
# 本文件只是模板，名字叫 .env.example；两个真正的 .env 都在 .gitignore 里。

AI_GATEWAY_API_KEY=vck_replace_me

# 可选：换网关根地址（默认 https://ai-gateway.vercel.sh/v4/ai，会拼上 /evaluation-model）
# AI_GATEWAY_BASE_URL=

# 可选：换环境变量名字（preset 行里配 apiKeyEnv 时才需要）
# JEV_API_KEY_ENV=AI_GATEWAY_API_KEY
